# yaml-language-server: $schema=../../schemas/service.json
dashboard:
  - widgets:
      - type: ${service.identity.service}
        password: ${service.runtime.credentials.password}
        url: ${service.urls.default.href}
        username: ${service.identity.username}
        version: 2
data:
  agent_listen: 45876
  agent_public_key: ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIPkxnKuxsVqSVZVXQ3OGHjZZFMAAclU/xLrEDdwOyRt+
  oidc_pkce_enabled: true
  oidc_callback_urls:
    - /api/oauth2-redirect
features:
  mail: true
  object_storage: true
  oidc: true
  password: true
identity:
  description: System Monitoring
  group: Infrastructure
  service: beszel
  title: Beszel
  username: max@excloo.com
routing:
  backend_port: 30333
targets:
  au-truenas:
    truenas:
      catalog_app: beszel-hub
      env:
        USER_CREATION: true
