# yaml-language-server: $schema=../../schemas/service.json
dashboard:
  - name: ${service.identity.title}
    description: ${service.identity.description}
    group: ${service.identity.group}
    href: ${service.urls.external.href}
    icon: ${service.identity.service}
    siteMonitor: ${service.urls.internal.href}
identity:
  description: AI Coding Agent
  group: Applications
  name: opencode
  service: opencode
  title: OpenCode
routing:
  backend_port: 4096
  backend_scheme: http
  urls:
    - expose: cloudflare
      url: opencode.excloo.com
      cloudflare_access:
        - name: OpenCode
          auto_redirect_to_identity: true
          allowed_idps:
            - excloo_id
          policies:
            - decision: allow
              include:
                - everyone: {}
targets:
  au-truenas: {}
