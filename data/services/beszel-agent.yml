# yaml-language-server: $schema=../../schemas/service.json
credentials:
  fields:
    system_id: {}
    token: {}
dashboard:
  - name: ${services.beszel.identity.title}
    description: ${services.beszel.identity.description}
    group: ${server.dashboard[0].name}
    href: ${services.beszel.urls.default.href}/system/${service.runtime.credentials.system_id}
    icon: ${services.beszel.identity.service}
    widgets:
      - type: beszel
        password: ${services.beszel.runtime.credentials.password}
        systemId: ${service.runtime.credentials.system_id}
        url: ${services.beszel.urls.default.href}
        username: ${defaults.organization.email}
        version: 2
        fields:
          - status
          - cpu
          - memory
          - network
data:
  hub_url: ${services.beszel.urls.default.href}
  listen: 45876
  public_key: ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIPkxnKuxsVqSVZVXQ3OGHjZZFMAAclU/xLrEDdwOyRt+
identity:
  description: Resource Monitoring Agent
  name: beszel-agent
  service: beszel-agent
  title: Beszel Agent
imports:
  services:
    beszel: auto
targets:
  au-haos: {}
  au-hsp: {}
  au-truenas: {}
  us-hotdog: {}
