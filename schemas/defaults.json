{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "description": "Default field values deep-merged into every server, service, and DNS record before processing. Loaded into local.defaults alongside data/config.yml.",
  "required": ["credentials", "dns", "servers", "services", "targets"],
  "title": "Defaults",
  "type": "object",
  "properties": {
    "cloudflare": {
      "additionalProperties": false,
      "description": "Default Cloudflare values deep-merged into the config. identity_providers defaults to an empty map so HCL can safely iterate when no IDPs are configured.",
      "type": "object",
      "properties": {
        "access": {
          "additionalProperties": false,
          "description": "Default Zero Trust Access values deep-merged into the cloudflare config.",
          "type": "object",
          "properties": {
            "identity_providers": {
              "description": "Default empty map; config.yml overrides with actual IDP aliases.",
              "type": "object",
              "additionalProperties": {
                "additionalProperties": false,
                "description": "Cloudflare Access identity provider metadata.",
                "required": ["client_name", "display_name", "provider"],
                "type": "object",
                "properties": {
                  "client_name": {
                    "description": "Display name for the OIDC client in the identity provider.",
                    "type": "string"
                  },
                  "display_name": {
                    "description": "Display name for the managed Cloudflare Access identity provider.",
                    "type": "string"
                  },
                  "provider": {
                    "description": "Identity provider integration used to provision the Cloudflare Access OIDC client.",
                    "enum": ["pocket_id"],
                    "type": "string"
                  }
                }
              },
              "propertyNames": {
                "pattern": "^[a-z][a-z0-9_]*$"
              }
            }
          }
        }
      }
    },
    "credentials": {
      "additionalProperties": false,
      "description": "Default shapes for credential fields and typed generators.",
      "required": ["ro", "rw", "x509"],
      "type": "object",
      "properties": {
        "ro": {
          "additionalProperties": false,
          "description": "Default shape for read-only credential fields generated by a provider and read back from 1Password.",
          "required": ["mode", "purpose", "type"],
          "type": "object",
          "properties": {
            "mode": {
              "description": "Read-only: value is written from a provider-generated secret.",
              "enum": ["ro"],
              "type": "string"
            },
            "purpose": {
              "description": "Optional 1Password field purpose.",
              "enum": ["PASSWORD", null],
              "type": ["string", "null"]
            },
            "type": {
              "description": "1Password field type when purpose is not set.",
              "enum": ["CONCEALED", "STRING", null],
              "type": ["string", "null"]
            }
          }
        },
        "rw": {
          "additionalProperties": false,
          "description": "Default shape for read-write credential fields written to 1Password for manual entry and read back at apply time.",
          "required": ["mode", "purpose", "type"],
          "type": "object",
          "properties": {
            "mode": {
              "description": "Read-write: value may be supplied or overridden via 1Password.",
              "enum": ["rw"],
              "type": "string"
            },
            "purpose": {
              "description": "Optional 1Password field purpose.",
              "enum": ["PASSWORD", null],
              "type": ["string", "null"]
            },
            "type": {
              "description": "1Password field type when purpose is not set.",
              "enum": ["CONCEALED", "STRING", null],
              "type": ["string", "null"]
            }
          }
        },
        "x509": {
          "additionalProperties": false,
          "description": "Default values for provider-generated X.509 credential pairs.",
          "required": ["validity_period_hours"],
          "type": "object",
          "properties": {
            "validity_period_hours": {
              "description": "Certificate validity in hours.",
              "minimum": 1,
              "type": "integer"
            }
          }
        }
      }
    },
    "dns": {
      "additionalProperties": false,
      "description": "Default values merged into all auto-generated DNS records. Individual records may override these.",
      "required": ["priority", "proxied", "ttl", "wildcard"],
      "type": "object",
      "properties": {
        "priority": {
          "description": "Default MX/SRV record priority. Null omits the field for non-priority record types.",
          "minimum": 0,
          "type": ["integer", "null"]
        },
        "proxied": {
          "description": "Whether to proxy records through Cloudflare by default. Individual service records override this based on their routing.expose setting.",
          "type": "boolean"
        },
        "ttl": {
          "description": "Default DNS TTL in seconds. Use 1 for Cloudflare's automatic TTL.",
          "minimum": 1,
          "type": "integer"
        },
        "wildcard": {
          "description": "Whether to create a wildcard CNAME (*.hostname \u2192 hostname) alongside each auto-generated A/AAAA/CNAME record by default.",
          "type": "boolean"
        }
      }
    },
    "servers": {
      "description": "Default field values deep-merged into every server definition before processing. Keys mirror the server schema.",
      "type": "object"
    },
    "services": {
      "description": "Default field values deep-merged into every service definition before processing. Keys mirror the service schema (excluding `targets`, which carries per-deployment data only).",
      "type": "object"
    },
    "targets": {
      "additionalProperties": false,
      "description": "Per-platform defaults merged into each service deployment target during expansion (services_input.tf).",
      "required": ["fly", "truenas"],
      "type": "object",
      "properties": {
        "fly": {
          "description": "Defaults for Fly.io deployments.",
          "type": "object"
        },
        "truenas": {
          "description": "Defaults for TrueNAS catalog app deployments.",
          "type": "object"
        }
      }
    }
  }
}
