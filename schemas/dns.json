{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "description": "Manual DNS records for a Cloudflare-managed zone. Auto-generated records (servers, services, wildcards, ACME delegation) are not defined here.",
  "required": ["name"],
  "title": "DNS Zone",
  "type": "object",
  "properties": {
    "name": {
      "description": "Domain name of the Cloudflare zone (e.g. 'excloo.net'). Must match a zone in the Cloudflare account.",
      "pattern": "^(?=.{1,253}$)([a-z0-9](?:[a-z0-9-]{0,61}[a-z0-9])?\\.)+[a-z]{2,}$",
      "type": "string"
    },
    "records": {
      "description": "Manual DNS records to create in this zone. These are merged with the dns defaults (ttl, proxied, priority) before being applied.",
      "type": "array",
      "items": {
        "additionalProperties": false,
        "required": ["name", "content", "type"],
        "type": "object",
        "allOf": [
          {
            "if": {
              "properties": {
                "type": {
                  "enum": ["MX", "NS", "TXT"]
                }
              }
            },
            "then": {
              "properties": {
                "proxied": {
                  "const": false
                },
                "wildcard": {
                  "const": false
                }
              }
            }
          }
        ],
        "properties": {
          "content": {
            "description": "Record value. For A records: an IPv4 address. For AAAA: an IPv6 address. For CNAME/MX/NS: a hostname. For TXT: a quoted string.",
            "type": "string"
          },
          "id": {
            "description": "Stable identifier for records that share the same type and name. Changing this value recreates the OpenTofu resource address.",
            "type": "string"
          },
          "name": {
            "description": "DNS record name relative to the zone (e.g. 'www', 'mail'). Use '@' for the zone apex.",
            "type": "string"
          },
          "priority": {
            "description": "Record priority. Required for MX and SRV records; ignored for other types.",
            "minimum": 0,
            "type": "integer"
          },
          "proxied": {
            "description": "Whether to proxy this record through Cloudflare. Overrides the dns default. Cannot be used with TXT, MX, or NS records.",
            "type": "boolean"
          },
          "ttl": {
            "description": "TTL in seconds. Overrides the dns default. Use 1 for Cloudflare's automatic TTL (only valid when proxied is false).",
            "minimum": 1,
            "type": "integer"
          },
          "type": {
            "description": "DNS record type.",
            "enum": ["A", "AAAA", "CNAME", "MX", "NS", "SRV", "TXT"],
            "type": "string"
          },
          "wildcard": {
            "description": "Also create a wildcard CNAME (*.name → name) for this record. Overrides the dns default. Only applies to A, AAAA, and CNAME records.",
            "type": "boolean"
          }
        }
      }
    }
  }
}
