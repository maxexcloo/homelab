{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "description": "A physical machine, VM, router, or appliance managed by this homelab. Servers are the deployment targets for services.",
  "required": ["identity", "platform", "type"],
  "title": "Server",
  "type": "object",
  "definitions": {
    "credential_field": {
      "additionalProperties": false,
      "type": "object",
      "allOf": [
        {
          "if": {
            "required": ["bootstrap_type"]
          },
          "then": {
            "required": ["bootstrap_length"]
          },
          "else": {
            "not": {
              "required": ["bootstrap_length"]
            }
          }
        }
      ],
      "properties": {
        "bootstrap_length": {
          "description": "Byte count for hex/base64 types; character count for string/alphanumeric types. Omit for manually supplied 1Password fields.",
          "minimum": 1,
          "type": "integer"
        },
        "bootstrap_type": {
          "description": "Initial generated secret format. Omit to create an empty concealed 1Password field; hex/base64/string/alphanumeric bootstrap generated values and then read back from 1Password.",
          "enum": ["alphanumeric", "base64", "hex", "string"],
          "type": "string"
        },
        "mode": {
          "description": "Whether 1Password may be the source of truth for this field. rw fields create placeholders; ro fields are written from provider-generated values.",
          "enum": ["ro", "rw", null],
          "type": ["string", "null"]
        },
        "purpose": {
          "description": "Optional 1Password field purpose.",
          "enum": ["PASSWORD", null]
        },
        "type": {
          "description": "1Password field type when purpose is not set.",
          "enum": ["CONCEALED", "STRING", null]
        }
      }
    },
    "dashboard": {
      "description": "Homepage server card data. Values are rendered directly into Homepage YAML after template interpolation; `group` and `name` are used to place the card.",
      "properties": {},
      "type": "object",
      "additionalProperties": {
        "$ref": "#/definitions/json_data"
      }
    },
    "json_data": {
      "description": "Arbitrary JSON-compatible value.",
      "anyOf": [
        {
          "type": "boolean"
        },
        {
          "type": "number"
        },
        {
          "type": "string"
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/definitions/json_data"
          }
        },
        {
          "type": "object",
          "additionalProperties": {
            "$ref": "#/definitions/json_data"
          }
        },
        {
          "type": "null"
        }
      ]
    }
  },
  "properties": {
    "credentials": {
      "additionalProperties": false,
      "description": "Credential fields exposed as runtime.credentials.{name}. Omit bootstrap_type for manually supplied 1Password fields.",
      "type": "object",
      "properties": {
        "fields": {
          "description": "Credential fields keyed by field name.",
          "type": "object",
          "additionalProperties": {
            "$ref": "#/definitions/credential_field"
          },
          "propertyNames": {
            "pattern": "^[a-z0-9_]+$"
          }
        }
      }
    },
    "dashboard": {
      "description": "Homepage dashboard cards for this server. Null or omitted uses the auto-generated server card; [] renders no cards. Card objects are passed through to Homepage after template interpolation.",
      "type": ["array", "null"],
      "items": {
        "$ref": "#/definitions/dashboard"
      }
    },
    "data": {
      "description": "Arbitrary JSON-compatible data exposed to templates as server.data.",
      "type": "object",
      "additionalProperties": {
        "$ref": "#/definitions/json_data"
      }
    },
    "features": {
      "additionalProperties": false,
      "description": "Feature flags that provision resources, credentials, or generated setup artifacts for this server.",
      "type": "object",
      "properties": {
        "b2": {
          "description": "Provision a Backblaze B2 bucket and application key. Exposes runtime.attributes.{b2_application_key_id, b2_bucket_name, b2_endpoint} and runtime.credentials.b2_application_key.",
          "type": "boolean"
        },
        "cloud_init": {
          "description": "Generate cloud-init configuration for this server. Exposes the cloud_config output for Incus containers and OCI VMs.",
          "type": "boolean"
        },
        "cloudflare_acme": {
          "description": "Create a scoped Cloudflare API token with Zone DNS Edit permissions on the ACME domain. Exposes runtime.credentials.cloudflare_acme_token for DNS-01 challenge clients.",
          "type": "boolean"
        },
        "cloudflare_acme_legacy": {
          "description": "Create a scoped Cloudflare API token with Zone DNS Edit permissions on the internal and external domains. Exposes runtime.credentials.cloudflare_acme_legacy_token for DNS-01 challenge clients that cannot follow ACME CNAME delegation.",
          "type": "boolean"
        },
        "cloudflare_zero_trust_tunnel": {
          "description": "Create a Cloudflare Zero Trust tunnel and a read-only tunnel API token. Exposes runtime.attributes.cloudflare_tunnel_id and runtime.credentials.{cloudflare_tunnel_read_token, cloudflare_tunnel_token}. Services with routing.expose set to 'cloudflare' route through this tunnel.",
          "type": "boolean"
        },
        "cockpit": {
          "description": "Install Cockpit in generated setup artifacts and enable cockpit.socket for browser-based server administration.",
          "type": "boolean"
        },
        "docker": {
          "description": "This server runs Docker and can host Komodo-managed stacks. Adds the server to the Komodo server list.",
          "type": "boolean"
        },
        "monitoring": {
          "description": "Include this server in generated monitoring configuration. Does not provision resources or expose template variables.",
          "type": "boolean"
        },
        "monitoring_alerts": {
          "description": "Attach generated monitoring alerts to this server when monitoring is enabled. Does not provision resources or expose template variables.",
          "type": "boolean"
        },
        "password": {
          "description": "Generate a random admin password. Exposes runtime.credentials.password and runtime.credentials.password_hash (bcrypt). Stored in 1Password.",
          "type": "boolean"
        },
        "pushover": {
          "description": "Create 1Password read/write fields for this server's Pushover application token and user/group key. Exposes runtime.credentials.pushover_application_token and runtime.credentials.pushover_user_key.",
          "type": "boolean"
        },
        "resend": {
          "description": "Provision a Resend API key for outbound email. Exposes runtime.credentials.resend_api_key.",
          "type": "boolean"
        },
        "tailscale": {
          "description": "Generate a Tailscale auth key. Exposes runtime.credentials.tailscale_auth_key for use in generated setup artifacts.",
          "type": "boolean"
        },
        "zfs": {
          "description": "Install Sanoid and ZFS administration packages in generated setup artifacts. Does not provision resources or expose template variables.",
          "type": "boolean"
        }
      }
    },
    "identity": {
      "additionalProperties": false,
      "description": "Human-readable identifiers for this server.",
      "required": ["name", "region", "title"],
      "type": "object",
      "properties": {
        "description": {
          "description": "Optional longer description of this server's purpose.",
          "type": "string"
        },
        "name": {
          "description": "Short hostname component used to build the server FQDN. For region-prefixed keys (e.g. au-malatang) this is the part after the hyphen (malatang). Must be lowercase alphanumeric or hyphen.",
          "pattern": "^[a-z0-9-]+$",
          "type": "string"
        },
        "region": {
          "description": "Two-letter region code (e.g. au, us). Combined with identity.name to form the FQDN: {name}.{region}.{domain}.",
          "pattern": "^[a-z]{2}$",
          "type": "string"
        },
        "title": {
          "description": "Display name shown in dashboards, Komodo and 1Password (e.g. 'Malatang').",
          "type": "string"
        },
        "username": {
          "description": "Primary admin username for SSH access. Stored in 1Password alongside the generated password.",
          "type": "string"
        }
      }
    },
    "networking": {
      "additionalProperties": false,
      "description": "Network addressing for this server. Public IPs are used for external DNS records; Tailscale IPs are resolved automatically from the tailnet.",
      "type": "object",
      "properties": {
        "management_host": {
          "description": "Hostname or IP for management access (e.g. Incus API endpoint). Used by Komodo as the server address.",
          "pattern": "^$|^(?=.{1,253}$)([a-z0-9](?:[a-z0-9-]{0,61}[a-z0-9])?\\.)*[a-z0-9](?:[a-z0-9-]{0,61}[a-z0-9])?$|^([0-9]{1,3}\\.){3}[0-9]{1,3}$|^[0-9A-Fa-f:]+$",
          "type": "string"
        },
        "management_port": {
          "description": "Port for management access. Defaults to 443.",
          "maximum": 65535,
          "minimum": 1,
          "type": "integer"
        },
        "public_host": {
          "description": "Public CNAME target (e.g. a dynamic DNS hostname). Creates a CNAME external DNS record instead of A/AAAA. Inherited from the parent chain up to 2 levels.",
          "pattern": "^$|^(?=.{1,253}$)([a-z0-9](?:[a-z0-9-]{0,61}[a-z0-9])?\\.)+[a-z]{2,}$",
          "type": "string"
        },
        "public_ipv4": {
          "description": "Public IPv4 address. Creates an A record on the external domain. Inherited from the parent chain up to 2 levels.",
          "pattern": "^$|^((25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\\.){3}(25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)$",
          "type": "string"
        },
        "public_ipv6": {
          "description": "Public IPv6 address (bare, without prefix length). Creates an AAAA record on the external domain. Inherited from the parent chain up to 2 levels.",
          "pattern": "^$|^[0-9A-Fa-f:]+$",
          "type": "string"
        },
        "ssh_port": {
          "description": "Port for SSH access. Defaults to 22.",
          "maximum": 65535,
          "minimum": 1,
          "type": "integer"
        }
      }
    },
    "parent": {
      "description": "Key of the parent server (e.g. 'au'). Public IPs are inherited from the parent chain up to 2 levels deep, allowing VMs and containers to share the host's public address.",
      "pattern": "^$|^[a-z0-9-]+$",
      "type": "string"
    },
    "platform": {
      "description": "Provisioning platform. Controls which Terraform resources are created. incus = Incus container/VM; oci = Oracle Cloud instance; truenas = TrueNAS app; unifi = UniFi device; unmanaged = externally managed (DNS and credentials only).",
      "enum": ["incus", "oci", "truenas", "unifi", "unmanaged"],
      "type": "string"
    },
    "platform_config": {
      "additionalProperties": false,
      "description": "Platform-specific configuration. Only the block matching the server's platform is used.",
      "type": "object",
      "properties": {
        "incus": {
          "additionalProperties": false,
          "description": "Configuration for Incus-managed containers and VMs.",
          "type": "object",
          "properties": {
            "cpus": {
              "description": "Number of vCPUs to allocate (limits.cpu).",
              "minimum": 1,
              "type": "integer"
            },
            "disks": {
              "description": "Additional disk devices to attach to this instance.",
              "type": "array",
              "items": {
                "additionalProperties": false,
                "required": ["path", "pool"],
                "type": "object",
                "properties": {
                  "name": {
                    "description": "Device name. Defaults to disk-{n} if omitted.",
                    "type": "string"
                  },
                  "path": {
                    "description": "Mount path inside the instance (e.g. '/data').",
                    "type": "string"
                  },
                  "pool": {
                    "description": "Incus storage pool name.",
                    "type": "string"
                  },
                  "size": {
                    "description": "Disk size in GiB.",
                    "minimum": 1,
                    "type": "integer"
                  }
                }
              }
            },
            "image": {
              "description": "Incus image to use (e.g. 'ubuntu:24.04'). Null uses the platform default.",
              "type": ["string", "null"]
            },
            "memory": {
              "description": "RAM in GiB to allocate (limits.memory).",
              "minimum": 1,
              "type": "integer"
            },
            "nested": {
              "description": "Enable nested containerisation (security.nesting). Only applies to containers.",
              "type": "boolean"
            },
            "networks": {
              "description": "Network interface devices to attach to this instance.",
              "type": "array",
              "items": {
                "additionalProperties": false,
                "required": ["network"],
                "type": "object",
                "properties": {
                  "mac_address": {
                    "description": "Fixed MAC address (hwaddr). Leave unset for a random address.",
                    "pattern": "^([0-9A-Fa-f]{2}:){5}[0-9A-Fa-f]{2}$",
                    "type": "string"
                  },
                  "name": {
                    "description": "Device name. Defaults to eth-{n} if omitted.",
                    "type": "string"
                  },
                  "network": {
                    "description": "Incus network to attach to (e.g. 'br0').",
                    "type": "string"
                  }
                }
              }
            },
            "pci_devices": {
              "description": "PCI devices to pass through to this instance.",
              "type": "array",
              "items": {
                "additionalProperties": false,
                "required": ["address"],
                "type": "object",
                "properties": {
                  "address": {
                    "description": "PCI address of the device (e.g. '0000:03:00.0').",
                    "type": "string"
                  },
                  "name": {
                    "description": "Device name. Defaults to pci-{n} if omitted.",
                    "type": "string"
                  }
                }
              }
            },
            "privileged": {
              "description": "Run as a privileged container (security.privileged). Only applies to containers.",
              "type": "boolean"
            },
            "profiles": {
              "description": "Incus profiles to apply to this instance. Defaults to ['default'].",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "protection": {
              "description": "Enable deletion protection (security.protection.delete).",
              "type": "boolean"
            },
            "secureboot": {
              "description": "Enable Secure Boot (security.secureboot). Only applies to virtual machines.",
              "type": "boolean"
            },
            "type": {
              "description": "Incus instance type.",
              "enum": ["container", "virtual-machine"],
              "type": "string"
            },
            "usb_devices": {
              "description": "USB devices to pass through to this instance.",
              "type": "array",
              "items": {
                "additionalProperties": false,
                "required": ["productid", "vendorid"],
                "type": "object",
                "properties": {
                  "mode": {
                    "description": "File mode for the device node (e.g. '0666').",
                    "type": "string"
                  },
                  "name": {
                    "description": "Device name. Defaults to usb-{n} if omitted.",
                    "type": "string"
                  },
                  "productid": {
                    "description": "USB product ID as a 4-digit hex string (e.g. '1234').",
                    "pattern": "^[0-9a-fA-F]{4}$",
                    "type": "string"
                  },
                  "vendorid": {
                    "description": "USB vendor ID as a 4-digit hex string (e.g. '5678').",
                    "pattern": "^[0-9a-fA-F]{4}$",
                    "type": "string"
                  }
                }
              }
            }
          }
        },
        "oci": {
          "additionalProperties": false,
          "description": "Configuration for Oracle Cloud Infrastructure instances.",
          "type": "object",
          "properties": {
            "assign_public_ip": {
              "description": "Assign a public IPv4 address to the primary VNIC.",
              "type": "boolean"
            },
            "cpus": {
              "description": "Number of OCPUs to allocate.",
              "minimum": 1,
              "type": "integer"
            },
            "disk_size": {
              "description": "Boot volume size in GiB.",
              "minimum": 1,
              "type": "integer"
            },
            "image_id": {
              "description": "OCI image OCID to use as the boot volume source.",
              "pattern": "^$|^ocid1\\.image\\.",
              "type": "string"
            },
            "ingress_ports": {
              "description": "TCP and UDP ports open in the network security group ingress rules. Defaults to [22, 80, 443].",
              "type": "array",
              "uniqueItems": true,
              "items": {
                "maximum": 65535,
                "minimum": 1,
                "type": "integer"
              }
            },
            "memory": {
              "description": "RAM in GiB to allocate.",
              "minimum": 1,
              "type": "integer"
            },
            "shape": {
              "description": "OCI compute shape (e.g. 'VM.Standard.A1.Flex').",
              "type": "string"
            }
          }
        }
      }
    },
    "type": {
      "description": "Functional role of the server. Used for grouping in monitoring (Gatus) and display. appliance = dedicated device; cicd = CI/CD runner; ephemeral = temporary workload; router = network gateway; server = general-purpose machine; vm = virtual machine.",
      "enum": ["appliance", "cicd", "ephemeral", "router", "server", "vm"],
      "type": "string"
    }
  }
}
