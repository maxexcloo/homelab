[env]
API_DATA_IS_SENSITIVE = "true"
RENDER_DIR = ".render"
TF_CLI_ARGS_apply = "-parallelism=20"
TF_CLI_ARGS_plan = "-parallelism=20"

[tools]
opentofu = "1.12.1"
prettier = "3.8.4"
ruff = "0.15.16"
sops = "3.13.1"
uv = "0.11.19"

# Setup
[tasks.init]
description = "Initialize OpenTofu"
run = "tofu init"

[tasks.setup]
description = "Initial project setup"
run = """
# Setting up project...
if [[ ! -f .mise.local.toml ]]; then
  echo "Creating .mise.local.toml template..."
  cp .mise.local.toml.default .mise.local.toml
  echo "Created .mise.local.toml - please add your tokens"
  exit 1
fi
"""

# Development
[tasks.check]
description = "Format check, lint, and validate"
depends = ["lint", "test", "validate"]
run = """
prettier --check "data/**/*.yml" "schemas/*.json" "templates/**/*.yml"
ruff format --check scripts modules templates/workflows tests
tofu fmt -check -recursive
"""

[tasks.fmt]
description = "Format all files"
run = """
prettier --log-level warn --write "data/**/*.yml" "schemas/*.json" "templates/**/*.yml"
ruff check --fix --select I scripts modules templates/workflows tests
ruff format scripts modules templates/workflows tests
tofu fmt -recursive
"""

[tasks.lint]
description = "Validate source and default-merged YAML against JSON schemas"
depends = ["sort-check"]
run = """
uv run scripts/validate_data.py
ruff check --extend-select I scripts modules templates/workflows tests
"""

[tasks.test]
description = "Run unit tests"
run = "uv run python -m unittest discover -s tests"

[tasks.validate]
description = "Validate configurations"
run = "tofu validate"

# Deployment
[tasks.apply]
description = "Apply all changes"
run = 'tofu apply'

[tasks.plan]
description = "Plan all changes"
run = "tofu plan"

# Scripts
[tasks.render]
description = "Render plaintext deployment artifacts via debug_dir"
run = "tofu plan -refresh=false -var debug_dir=$RENDER_DIR"

[tasks.sort-check]
description = "Check YAML and JSON Schema key ordering"
run = "uv run scripts/sort_check.py"
