{
  "values": {
    "linkwarden": {
      "db_password": "${service.db_password_sensitive}",
      "meilisearch_master_key": "${service.meilisearch_master_key_sensitive}",
      "nextauth_secret": "${service.nextauth_secret_sensitive}",
      "postgres_image_selector": "postgres_18_image"
    },
    "network": {
      "web_port": {
        "bind_mode": "published",
        "port_number": ${service.networking.port}
      }
    },
    "storage": {
      "data": {
        "ix_volume_config": {
          "acl_enable": false,
          "dataset_name": "data"
        },
        "type": "ix_volume"
      },
      "meili_data": {
        "ix_volume_config": {
          "acl_enable": false,
          "dataset_name": "meili_data"
        },
        "type": "ix_volume"
      },
      "postgres_data": {
        "ix_volume_config": {
          "acl_enable": false,
          "dataset_name": "postgres_data"
        },
        "type": "ix_volume"
      }
    }
  }
}
