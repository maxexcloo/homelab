%{~ for server_key, server in servers ~}
%{~ if server.features.docker ~}
[[server]]
description = "${server.description}"
name = "${server_key}"

[server.config]
address = "https://${server.fqdn_internal}:8120"
enabled = true
region = "${server.identity.region}"

%{~ endif ~}
%{~ endfor ~}
