${join("\n", compact([
  service.features.pushover ? "PUSHOVER_APPLICATION_TOKEN=\"${service.runtime.secrets.pushover_application_token}\"" : "",
  service.features.pushover ? "PUSHOVER_USER_KEY=\"${service.runtime.secrets.pushover_user_key}\"" : "",
  service.features.resend ? "RESEND_API_KEY=\"${service.runtime.secrets.resend_api_key}\"" : "",
  service.features.tailscale ? "TAILSCALE_AUTH_KEY=\"${service.runtime.secrets.tailscale_auth_key}\"" : ""
]))}
