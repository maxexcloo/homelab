{
  "values": {
    "aiostreams": {
      "base_url": "https://${service.fqdn_internal}",
      "secret_key": "${service.state.secrets.secret_key}"
    },
    "network": {
      "web_port": {
        "bind_mode": "published",
        "port_number": ${service.networking.port}
      }
    },
    "storage": {
      "data": {
        "ix_volume_config": {
          "acl_enable": false,
          "dataset_name": "data"
        },
        "type": "ix_volume"
      }
    }
  }
}
