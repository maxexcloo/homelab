{
  "values": {
    "aiostreams": {
      "base_url": "${service.url}",
      "secret_key": "${service.state.secrets.secret_key}"
    },
    "network": {
      "web_port": {
        "bind_mode": "published",
        "port_number": ${service.routing.port}
      }
    },
    "storage": {
      "data": {
        "ix_volume_config": {
          "acl_enable": false,
          "dataset_name": "data"
        },
        "type": "ix_volume"
      }
    }
  }
}
